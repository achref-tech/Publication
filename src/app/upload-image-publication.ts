import { Byte } from "@angular/compiler/src/util";
import { Publication } from "./publication";

export class UploadImagePublication {
    id:number;
    name:string;
    data:Byte;
    publication:Publication;
    dataValue:String;
    
}
