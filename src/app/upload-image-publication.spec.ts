import { UploadImagePublication } from './upload-image-publication';

describe('UploadImagePublication', () => {
  it('should create an instance', () => {
    expect(new UploadImagePublication()).toBeTruthy();
  });
});
